#include <iostream>  // included standard library for printing on screen

int main(){ 
    // statement

    std::cout << "Hello World" ;
    
    // cout is Character out

    return 0; // why 0, it tells the OS that the program terminate correctly
}